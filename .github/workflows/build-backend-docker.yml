name: Build Backend Docker & Push

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.config.os }}
    strategy:
      fail-fast: false
      matrix:
        config:
          - os: ubuntu-latest
            platform: linux
            arch: amd64
            bun_arch: x64
          - os: ubuntu-latest
            platform: linux
            arch: arm64
            bun_arch: arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: ./backend
        run: bun install

      - name: Build the project
        working-directory: ./backend
        run: |
          bun build --minify --sourcemap src/index.ts --compile --target=bun-${{ matrix.config.platform }}-${{ matrix.config.bun_arch }} --outfile ./dist/backend-${{ matrix.config.platform }}-${{ matrix.config.bun_arch }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          platforms: linux/${{ matrix.config.arch }}
          push: true
          tags: createchstudio/vgorc-tm-backend:${{ matrix.config.arch }}
          context: backend
          file: backend/Dockerfile-${{ matrix.config.bun_arch }}